require 'spec_helper'

describe Rubocop::Gitdiff do
  it 'has a version number' do
    expect(Rubocop::Gitdiff::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
